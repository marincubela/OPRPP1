package hr.fer.oprpp1.hw04.db.parser;

/**
 * Vrijednosti tipova tokena koje klasa {@link Token} može biti.
 * 
 * @author Cubi
 *
 */
public enum TokenType {
	ATTRIBUTE, OPERATOR, STRING, AND, END;
}